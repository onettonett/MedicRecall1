specVersion: "v1"
serviceId: "flashcard"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "flashcard-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
